<h1>Add a new list</h1>
<div class="container">
<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <div class="button">
    <%= f.submit "Add list", class: "btn btn-primary" %>
    <%= link_to "sommaire", lists_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
